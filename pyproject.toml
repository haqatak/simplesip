[project]
name = "sipbridge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pexpect>=4.9.0",
    "pyaudio>=0.2.14",
    "python-sip-client>=0.0.4",
    "ruff>=0.12.3",
]
